{
  "name": "@malink/has-session-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@malink/protocol": "file:../../packages/protocol"
  },
  "devDependencies": {
    "@types/node": ">=20",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.4"
  }
}
